{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "UserEmbeddingArtifact",
  "description": "Schema for User Embedding learned artifact, containing semantic embeddings for users.",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "Unique user identifier",
      "minLength": 1
    },
    "user_embedding": {
      "type": "array",
      "description": "Semantic embedding vector for the user",
      "items": {
        "type": "number"
      },
      "minItems": 1
    },
    "user_categories": {
      "type": "array",
      "description": "List of categories the user has reviewed",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1
    },
    "embedding_model": {
      "type": "string",
      "description": "Name of the embedding model used",
      "minLength": 1
    },
    "embedding_dimension": {
      "type": "integer",
      "description": "Dimension of the embedding vector",
      "minimum": 1
    },
    "embedding_text": {
      "type": "string",
      "description": "Text used to generate the embedding (optional)",
      "minLength": 1
    }
  },
  "required": ["user_id", "user_embedding", "user_categories", "embedding_model", "embedding_dimension"]
}

